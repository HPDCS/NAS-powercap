#!/bin/bash

max_cpu_id=31


#------------------------------------------------------------------------------------------------
#------------------------------------------------------------------------------------------------
cpu=0
while [ $cpu -le $max_cpu_id ] 
        do
	echo $1 > /sys/devices/system/cpu/cpu$cpu/cpufreq/scaling_setspeed
        cpu=$[cpu+1]
done

